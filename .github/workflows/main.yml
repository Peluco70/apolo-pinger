name: Ping PA
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping PA
        run: curl -sSf --max-time 15 https://JhonyTurbo.pythonanywhere.com/api/ping
